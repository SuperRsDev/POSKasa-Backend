const upiti = require('../upiti.servis');

exports.dajProizvodePoKategoriji = function(kategorija) {

    return Promise.all([])
        .then((data) => {

            return new Promise((res, _) => res([]));
        })
}
